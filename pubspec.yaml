name: testprovider
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <3.0.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  collection: ^1.16.0
  easy_localization: ^3.0.1 # Translations
  flutter:
    sdk: flutter
  flutter_adaptive_scaffold: ^0.0.8 # Route Related
  go_router: ^5.2.0 # Route Related
  hive: ^2.2.3 # Storage Related
  hive_flutter: ^1.1.0 # Storage Related
  provider: ^6.0.4 # State Manager
  url_launcher: ^6.1.6 # Route Related
  
dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
  assets:
    - assets/translations/